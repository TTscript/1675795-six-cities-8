import { Offers } from '../../mocks/types';
import { Reviews } from '../../mocks/types';


type AppScreenProps = {
  offersCount: number;
  offers: Offers,
  reviews: Reviews,
}

export type { AppScreenProps };

